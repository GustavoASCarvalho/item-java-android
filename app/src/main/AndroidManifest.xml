<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.biblioteca">

    <!-- Permissão de acesso básico à internet (boa prática, embora não estritamente necessária para este app) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <!-- Activity Principal (1/3) - Lista os tipos de coleções -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity de Itens (2/3) - Exibe a lista de itens da coleção selecionada (usa RecyclerView) -->
        <activity
            android:name=".ItemsActivity"
            android:label="@string/app_name"
            android:parentActivityName=".MainActivity" />

        <!-- Activity de Adicionar/Editar Item (3/3) - Formulário de persistência no SQLite -->
        <activity
            android:name=".AddItemActivity"
            android:label="@string/app_name"
            android:parentActivityName=".ItemsActivity" />

    </application>
</manifest>